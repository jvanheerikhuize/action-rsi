# Feature Specification: ${SPEC_TITLE}
# Generated by RSI Audit Agent on ${AUDIT_DATE}
# Validate against: a-sdlc/artifacts/schemas/feature-spec.schema.json

metadata:
  id: "${SPEC_ID}"
  title: "${SPEC_TITLE}"
  version: "1.0.0"
  status: "draft"
  priority: "${SPEC_PRIORITY}"
  author: "RSI Audit Agent"
  created_at: "${AUDIT_DATE}"
  updated_at: "${AUDIT_DATE}"
  tags: ${SPEC_TAGS}

description:
  summary: |
    ${SPEC_SUMMARY}

  problem_statement: |
    ${SPEC_PROBLEM}

  proposed_solution: |
    ${SPEC_SOLUTION}

  out_of_scope: ${SPEC_OUT_OF_SCOPE}

  dependencies: ${SPEC_DEPENDENCIES}

technical_requirements:
  constraints: ${SPEC_CONSTRAINTS}

  security: ${SPEC_SECURITY}

acceptance_criteria: ${SPEC_ACCEPTANCE_CRITERIA}

technical_notes: |
  ${SPEC_TECHNICAL_NOTES}

testing_requirements:
  unit_tests: ${SPEC_UNIT_TESTS}
  integration_tests: ${SPEC_INTEGRATION_TESTS}
  e2e_tests: false
  performance_tests: false
  test_scenarios: ${SPEC_TEST_SCENARIOS}

rollout:
  rollout_strategy: "manual"
  rollback_plan: |
    Revert the changes introduced by this spec. All modifications are
    scoped and reversible via git revert.
