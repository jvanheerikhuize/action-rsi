name: "RSI: Non-Functional Audit"

on:
  schedule:
    - cron: '0 6 * * 2'  # Tuesday 06:00 UTC
  workflow_dispatch:
    inputs:
      test_mode:
        description: 'Test mode (only audit test repos)'
        type: boolean
        default: true
      budget_usd:
        description: 'Budget cap in USD'
        type: string
        default: '2'

permissions:
  contents: write

jobs:
  non_functional:
    uses: ./.github/workflows/rsi-audit.yml
    with:
      dimensions: non_functional
      test_mode: ${{ inputs.test_mode || true }}
      budget_usd: ${{ inputs.budget_usd || '2' }}
    secrets: inherit
